# Event Logger

Event Logger captures event streams and writes them to logs.

## Transports

- D-Bus path events
- MQTT topic events

## Runtime

Use the start script and pass --mqtt to select MQTT logging.

Examples:

- D-Bus mode: start 1
- MQTT mode: start --mqtt 1
