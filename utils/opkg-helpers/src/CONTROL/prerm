#!/bin/bash

PACKAGENAME=opkg-helpers
source "/data/${PACKAGENAME}/opkg-common.sh"

SCRIPT="# ${PACKAGENAME} (do not edit)
/data/${PACKAGENAME}/rc.local.d/startup.sh"

remove_script_from_rc_local "${SCRIPT}"

remove_package_name_from_custom_packages_file "${PACKAGENAME}"

echo "successfully removed ${PACKAGENAME}"
