set -o nounset

this_path="$(dirname "${BASH_SOURCE[0]}")"

echo "copying /feeds/venus-data-opkg-manager-blind-install.tgz -> /run/media/sdb"
cp $this_path/../../../feeds/venus-data-opkg-manager-blind-install.tgz /run/media/sdb

echo done