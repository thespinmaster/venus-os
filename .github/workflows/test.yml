- name: Build static opkg site
  on:
    workflow_dispatch:
  run: |
    set -e

    html="$(cat <<'EOF'
    <html>
    <head>
      <meta charset='utf-8'>
      <title>Index</title>
    </head>
    </html>
EOF
)"

    echo "$html"
